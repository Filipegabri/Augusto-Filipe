{
    "name": "contact-form",
    "version": "1.0.0",
    "description": "Contact form with MongoDB and Netlify Functions",
    "scripts": {
        "start": "netlify dev",
        "test": "echo \"No tests specified\" && exit 0"
    },
    "dependencies": {
        "dotenv": "^16.5.0",
        "mongodb": "^6.16.0"
    },
    "devDependencies": {
        "netlify-cli": "^17.34.0"
    }
}
